## 相对于流水线并行（把模型“横切”开），**数据并行 (Data Parallelism, DP)** 是大模型训练中最基础、使用最广泛的并行技术。

它的核心逻辑是：**“模型不动，数据拆分，同步梯度。”**

---

### 1. 基本原理：多劳多得的“复印”模式

想象你有一本厚厚的作业（数据集）要写，一个人写太慢。数据并行的做法是：

1. **完全复制**：给每个同学（GPU）发一本一模一样的教科书（**模型参数的副本**）。

2. **分发任务**：把作业拆成几份，每个同学写不同的页码（**不同的 Batch 数据**）。

3. **对答案（同步）**：每个同学写完后，大家坐在一起讨论这一页的知识点（**交换梯度**），确信理解一致后，统一更新自己书上的内容，准备写下一页。


### 2. 执行步骤：从计算到通信

在一次训练迭代中，数据并行遵循以下流程：

1. **分发 (Scatter)**：将一个大的 Batch 切分为 N 份（N 为 GPU 数量），分给各张显卡。

2. **前向 + 反向计算**：每张显卡独立计算自己那部分数据的 Loss 和梯度（Gradient）。

此时，各卡的梯度是不一样的，因为看到的数据不同。

3. **规约 (All-Reduce)**：这是最关键的一步。所有 GPU 进行通信，将各自计算出的梯度取**平均值**。

4. **更新 (Update)**：每张显卡利用这个平均梯度，同步更新本地的模型参数。

更新完后，所有显卡上的模型依然是完全一致的。

---

### 3. 数据并行的两种进化形态

#### A. 传统数据并行 (DP)

* **特点**：通常指 PyTorch 里的 `DataParallel`。它有一个“主卡”负责分发和汇总。
* **缺点**：主卡通信压力极大，容易成为瓶颈，且不支持多机多卡。

#### B. 分布式数据并行 (DDP) - 主流方案

* **特点**：PyTorch 的 `DistributedDataParallel`。没有主卡，所有 GPU 平等通信（使用 Ring All-Reduce 算法）。
* **优势**：效率极高，通信开销随 GPU 增加线性增长，是目前工业界的标配。

#### C. ZeRO / 完全分片数据并行 (FSDP)

* **痛点**：虽然 DDP 很快，但每张卡都要存一份完整的模型参数、优化器状态，非常占显存。
* **进化**：ZeRO（DeepSpeed 核心技术）将参数、梯度、优化器状态也“切碎”存到不同卡上。计算时再临时“借”过来，算完就扔。这让数据并行也能跑起超大模型。

---

### 4. 数据并行 vs 流水线并行

| 维度 | 数据并行 (DP/DDP) | 流水线并行 (PP) |
| --- | --- | --- |
| **切分对象** | 切分**训练数据** | 切分**模型层数** |
| **模型副本** | 每张卡都有完整（或分片）模型 | 每张卡只有模型的一部分 |
| **通信内容** | 传输**梯度** | 传输**激活值和中间梯度** |
| **适用场景** | 模型能塞进单卡，想加速训练 | 模型太大，单卡装不下 |

---

### 5. 总结

数据并行解决的是**“算得慢”**的问题，通过增加算力来处理海量数据；而流水线并行解决的是**“装不下”**的问题。

在实际的大规模训练中（如训练 Llama 3），我们通常会把两者结合：

* **组内**：用流水线并行把模型拆开。
* **组间**：用数据并行把这个拆开的模型再复制多份，进一步提升吞吐量。

**想看看在 Megatron-LM 这种框架里，数据并行的代码是如何与你刚才看的流水线并行逻辑“缝合”在一起的吗？**